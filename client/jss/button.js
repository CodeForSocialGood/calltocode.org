import * as colors from './colors'
export const button = {
  root: {
    color: 'white',
    backgroundColor: colors.primaryColor,
    fontSize: '100%',
    border: 0,
    borderRadius: '5px',
    padding: '17px'
  }
}
